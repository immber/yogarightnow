<div id="results">
	<p>Here are the studio's in <%= @zip %>:</p>
	<ul>List of Results:
		<% @results.each do |result| %>
			<li><a href="<%= result.url %>"><%= result.name %></a></li>
		<% end %>
	</ul>
</div>